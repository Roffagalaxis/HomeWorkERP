<Window x:Class="WpfERP.TermekView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfERP"
        mc:Ignorable="d"
        Title="TermekView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="dbData" Grid.Row="0" IsReadOnly="True" Margin="7" HeadersVisibility="Column" />
        <Button x:Name="btnRefresh" Grid.Row="1" Content="Frissítés" HorizontalAlignment="Left" Width="75" Margin="7, 0, 0, 7" Click="BtnRefresh_Click" />
    </Grid>
</Window>
